package com.example.grupo6.scaleapp;

/*import android.support.annotation.NonNull;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

import com.firebase.ui.firestore.FirestoreRecyclerAdapter;
import com.firebase.ui.firestore.FirestoreRecyclerOptions;

public class AdaptadorPesosFirestoreUI extends
        FirestoreRecyclerAdapter<Peso, AdaptadorPeso.ViewHolder> {
    protected View.OnClickListener onClickListener;
    public AdaptadorPesosFirestoreUI(
            @NonNull FirestoreRecyclerOptions<Peso> options) {
        super(options);
    }
    @Override
    public AdaptadorPeso.ViewHolder onCreateViewHolder(
            ViewGroup parent, int viewType) {
        View view = LayoutInflater.from(parent.getContext())
                .inflate(R.layout.elementos, parent, false);
        return new AdaptadorPeso.ViewHolder(view);
    }
    @Override
    protected void onBindViewHolder(@NonNull AdaptadorPeso
            .ViewHolder holder, int position, @NonNull Peso peso) {
        AdaptadorPeso.personalizaVista(holder, peso);
        holder.itemView.setOnClickListener(onClickListener);
    }

}*/
